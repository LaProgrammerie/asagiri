# Changelog

All notable changes to `@laprogrammerie/asagiri` are documented here.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.1.0] - 2026-05-29

### Added

- `AsagiriClient` for local runtime REST API (`status`, sessions, events, memory list).
- TypeScript declarations via `tsc` build to `dist/`.
