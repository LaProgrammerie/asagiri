# Used by **downstream** clones (product repos): copy to `.cursor/template-sync.env` and set the
# absolute path to **this** template repository on disk so Cursor hooks can compare drift.
#
# Example:
# GENERIC_TEMPLATE_ROOT=/Users/you/Developer/Templates/Generic project

GENERIC_TEMPLATE_ROOT=
