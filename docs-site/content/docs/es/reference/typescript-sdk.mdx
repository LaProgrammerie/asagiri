---
title: SDK TypeScript
description: Cliente HTTP para la API local de runtime de Asagiri.
---

# SDK TypeScript (`@laprogrammerie/asagiri`)

Fuentes: `sdk/typescript/` en el repositorio Asagiri.

## Instalación

Desde npm (tags `sdk-v*`):

```bash
npm install @laprogrammerie/asagiri
```

Desde el repositorio:

```bash
cd sdk/typescript && npm install && npm run build
```

Publicación CI: workflow `sdk-npm-publish.yml`, secreto `NPM_TOKEN`.

## Ejemplo

```ts
import { AsagiriClient } from "@laprogrammerie/asagiri";

const runtime = new AsagiriClient({ baseUrl: "http://127.0.0.1:8765" });
const session = await runtime.startSession("onboarding-redesign", "workspace-saas", "onboarding");
await runtime.runFlow(session.id, "onboarding");
```

Primero: `asa runtime serve --port 8765`.

Ver [runtime serve](/docs/es/cli/runtime-serve).
