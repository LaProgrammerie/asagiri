---
title: TypeScript-SDK
description: HTTP-Client für die lokale Asagiri-Runtime-API.
---

# TypeScript-SDK (`@laprogrammerie/asagiri`)

Quellen: `sdk/typescript/` im Asagiri-Repository.

## Installation

Über npm (Tags `sdk-v*`):

```bash
npm install @laprogrammerie/asagiri
```

Aus dem Repository:

```bash
cd sdk/typescript && npm install && npm run build
```

CI-Veröffentlichung: Workflow `sdk-npm-publish.yml`, Secret `NPM_TOKEN`.

## Beispiel

```ts
import { AsagiriClient } from "@laprogrammerie/asagiri";

const runtime = new AsagiriClient({ baseUrl: "http://127.0.0.1:8765" });
const session = await runtime.startSession("onboarding-redesign", "workspace-saas", "onboarding");
await runtime.runFlow(session.id, "onboarding");
```

Zuerst: `asa runtime serve --port 8765`.

Siehe [runtime serve](/docs/de/cli/runtime-serve).
