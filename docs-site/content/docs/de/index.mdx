---
title: Asagiri
description: Deterministische Orchestrierung für KI-gestützte Coding-Workflows.
---

# Asagiri

Deterministische Orchestrierung für KI-gestützte Coding-Workflows.

Asagiri verwandelt Spezifikationen in nachvollziehbare, kostenbewusste Entwicklungsläufe. Die Pipeline verbindet lokale Voruntersuchung, Git-Worktrees, externe Coding-Agenten und von Ihnen definierte Validierungsbefehle; reproduzierbare Reports dokumentieren jeden Lauf. So bleibt nachvollziehbar, was passiert ist — statt eines undurchsichtigen Chat-Verlaufs.

```bash
asa work "develop billing-v2"
```

## Kernideen

**Local-first investigation:** Bevor Cloud-Modelle ins Spiel kommen, untersucht Asagiri das Repository mit begrenztem Grep und kontrolliertem Dateisystem-Durchlauf und verkleinert den Kontext. Standardmäßig landen nicht ganze Verzeichnisbäume bei Remote-APIs.

**Kosten- und Token-Schätzung:** Erwartete Ausgaben werden vor der Ausführung sichtbar. Heuristische Zähler und konfigurierbare Preisblätter liefern Größenordnungen; Budgets können Überschreitungen vor dem Agentenstart blockieren oder eine ausdrückliche Bestätigung einfordern.

**Git-Worktree-Isolation:** Jede Aufgabe läuft auf eigenem Branch und eigenem Arbeitsbaum unter `.asagiri/worktrees/`. Parallele Features stoßen seltener mit einem gemeinsamen Checkout zusammen.

**Agent-Routing:** Schrittklassen der Pipeline werden den Agentenprofilen aus Ihrer Konfiguration zugeordnet — etwa lokales Ollama für Triage, Cloud-CLIs für aufwendigere Implementierung — gesteuert durch Strategie und Policies in YAML.

**Validierung vor Vertrauen:** Asagiri führt **Ihre** Befehle aus (`go test`, Linter, eigene Skripte), nicht die Selbsteinschätzung des Agenten. Ein Schritt gilt erst nach erfolgreichen Checks als abgeschlossen.

**Reproduzierbare Reports:** Markdown und JSON liegen unter `.asagiri/runs/<run-id>/`, damit Sie später prüfen können, was gelaufen ist, welche Kosten angefallen sind und was die Validierung gemeldet hat.

## Einstieg

Beim Onboarding eines Repositories diese Seiten der Reihe nach durchgehen:

1. [Installation](/docs/de/getting-started/installation)
2. [Schnellstart](/docs/de/getting-started/quickstart)
3. [Erster Workflow](/docs/de/getting-started/first-workflow)

<Callout type="warning">
Asagiri beweist keine Korrektheit. Validierung und menschliche Review bleiben für kritische Änderungen Pflicht.
</Callout>
