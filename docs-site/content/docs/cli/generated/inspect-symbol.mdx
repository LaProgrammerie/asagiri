---
title: Inspect Symbol · AgentFlow CLI
description: Trouver un symbole Go
---

CLI path: **`agentflow inspect symbol`**

## When to use

Use **inspect** when you need fast, offline answers about symbols, neighbouring tests, or a compact snapshot of unstaged Git changes without spawning remote agents.

> Original CLI synopsis: Trouver un symbole Go

## Usage

```bash
agentflow inspect symbol [flags]
```

## Options

| Flag | Scope | Default | Description |
| --- | --- | --- | --- |
| `--dry-run` | Inherited | `false` | Simuler les exécutions sans lancer d'agent ni commandes externes |

## Examples

```bash
agentflow inspect symbol
```

> Wire richer samples through **cobra.Command.Example** when the happy path warrants copy/paste-friendly automation beyond this scaffold.

## What happens internally

- Loads contextual services via **loadContext**, runs the investigative runner, then prints filenames where the queried Go identifier appears.

## Failure modes

- Cobra rejects unknown flags or arity mismatches before any repository side effects occur.
- AgentFlow cannot resolve scaffolding when the command is invoked outside an initialised checkout.
- Git probing fails when binaries are missing or repositories are shallow or incomplete.
- Symbol lookups can legitimately return empty output whenever indexes lag behind edits.

## Related commands

- [Inspect Diff](./inspect-diff.mdx)
- [Inspect Tests](./inspect-tests.mdx)
