version: "2"            # requis par golangci-lint v2

run:
  timeout: 5m
  tests: true
  go: "1.25"            # aligne l'analyse sur go.mod (go 1.25.0)

linters:
  enable:
    - errcheck
    - govet
    - ineffassign
    - staticcheck
    - unused
