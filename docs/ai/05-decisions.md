# Decisions (lightweight ADRs)

**Policy:** any decision that **durably** affects architecture, code standards, team workflows, or product invariants **must** be recorded here (even briefly). If it is not in this log, it is not considered decided for the repo.

**Sync:** if a durable decision changes architecture or standards, the affected files in `docs/ai/` (`02-architecture.md`, `03-standards.md`, `04-workflows.md`, etc.) **must be updated in the same change** (same PR / same batch of commits), not “when we get around to it”.

Suggested format per entry:

| ID | Date | Decision | Context | Consequences |
|----|------|----------|---------|--------------|
| ADR-001 | 2026-05-17 | Stack Go ; module racine ; code sous `application/` ; Docker local via Compose ; orchestration `Makefile` ; pas Castor ni Yoimachi dans le template | Adaptation du template AI Engineering depuis PHP/docker-starter | Doc, squelette `agentflow`, CI Go ; déploiement/IaC hors scope jusqu’à nouvelle ADR |
| ADR-002 | 2026-05-17 | État AgentFlow en **SQLite** via `modernc.org/sqlite` (`CGO_ENABLED=0`) ; schéma V1 : `schema_version`, `runs`, `tasks` ; migrations SQL embarquées (`embed`) | Tranche `agentflow-init` — fondation traçable avant worktrees/agents | `state.sqlite` sous `.agentflow/` (gitignored) ; pas de Postgres pour l’état local V1 |
| ADR-003 | 2026-05-17 | Agents via **subprocess** (`exec.Command`, pas de shell) ; mode **`--dry-run` / `AGENTFLOW_DRY_RUN=1`** pour CI et tests sans binaires kiro/cursor/codex | V1 workflow agentique reproductible | Config `agents.*` dans `config.yaml` ; logs factices en dry-run |
| ADR-004 | 2026-05-17 | **Worktree Git par tâche** sous `.agentflow/worktrees/` ; branche `agentflow/<feature>/<task-id>` | Isolation des modifications agent | `clean` avec filtres `--merged` / `--failed` |
| ADR-005 | 2026-05-17 | **Validation externe** : commandes depuis `validation.commands` (config) ou payload tâche ; défauts Go si `go.mod` | Principe §2.4 spec — l’agent ne valide pas seul | Pas de `composer` dans les défauts du template Go |
| ADR-006 | 2026-05-17 | **Modèle tâche canonique** dans `pkg/agentflow` ; persistance YAML+JSON sous `.agentflow/tasks/` | spec §8 | Statuts §8.2 distincts des statuts `runs` |
| ADR-007 | 2026-05-17 | **RAG local minimal** : `chunks.sqlite` + recherche LIKE ; pas d’embeddings en V1 | spec §10.3 | `agentflow index` ; enrich heuristique si index absent |
| ADR-008 | 2026-05-17 | **State machine** explicite dans `workflow/state_machine.go` ; `--force` ; `resume --execute` dry-run | spec §12 | Reprise hors dry-run = diagnostic uniquement |
| ADR-009 | 2026-05-17 | **Intent Layer** (`internal/intent`) : resolver hybride + planner composant les primitives V1 ; sources via `internal/source` ; repo = vérité exécutable | specv2 §3–6 | Notion jamais exécuté sans sync local ; `NOTION_TOKEN` via env |
| ADR-010 | 2026-05-17 | **V3 cost/perf** : packages `cost`, `contextopt`, `investigation`, `telemetry`, `tui` ; pipeline `RunV3Pipeline` ; prix **uniquement** via `pricing.models` (pas de hardcode) ; MCP stdio désactivé par défaut ; TUI isolée (`internal/tui`) | specv3 | Migration SQLite `run_metrics` / `step_metrics` ; `work --estimate-only` |
| ADR-011 | 2026-05-17 | **Consolidation post-V3** : spec active `spec-postv123.md` ; collecte contexte via candidats investigation (`CollectForPipeline`) ; LICENSE **Apache 2.0** LaProgrammerie ; explainability dans sorties estimate/work ; package `redact` | Ouverture OSS, réduction I/O et dette | Audits `docs/consolidation/` ; pas de breaking CLI V1–V3 |
| ADR-012 | 2026-05-17 | **Docs publiques** : `docs-site/` (Fumadocs + Next static export) ; référence CLI générée via `agentflow docs generate-cli` ; canon agents reste `docs/ai/` ; `basePath` `/asagiri` uniquement si `GITHUB_PAGES=true` (legacy) | `spec-doc.md` ; OSS international (EN) | ADR publics dans `docs/decisions/` ; hébergement : **ADR-014** |
| ADR-014 | 2026-05-17 | **Hébergement docs** : Cloudflare Pages (direct upload Wrangler) ; CI `.github/workflows/docs-cloudflare-pages.yml` (prod `main`, preview PR) + `.github/workflows/docs-check.yml` (build sans secrets) ; **pnpm** dans `docs-site/` ; workflow GitHub Pages `docs.yml` **retiré** | `spec-deploy-doc.md` ; custom domain / previews PR | Secrets repo : `CLOUDFLARE_API_TOKEN`, `CLOUDFLARE_ACCOUNT_ID`, `CLOUDFLARE_PAGES_PROJECT` ; pas de `GITHUB_PAGES` en CI Cloudflare |
| ADR-013 | 2026-05-17 | **Docs i18n** : Fumadocs `parser: dir` — `content/docs/{en,fr,de,es}/` ; URLs `/docs` (en) et `/docs/{fr,de,es}/…` ; sélecteur de langue (navbar Fumadocs) ; CLI Cobra générée **en anglais** sous `en/cli/generated/` ; FR/DE/ES : pages manuelles traduites + stubs narratifs pour commandes clés | `spec-doc.md` ; demande OSS multilingue | `TestPublicDocsNoPlaceholders` parcourt les 4 locales ; pas de `--locale` docgen en V1 |
| ADR-015 | 2026-05-17 | **Distribution release (AgentFlow)** : GitHub Releases sur **`LaProgrammerie/asagiri`** (pas de repo `agentflow` dédié) ; binaire **`agentflow`** ; **GoReleaser** ; archives nommées convention GoReleaser `agentflow_{OS}_{ARCH}` (ex. `agentflow_Darwin_arm64.tar.gz`, `.zip` Windows) ; **ldflags** inchangés sur le module `github.com/LaProgrammerie/asagiri/application/internal/version.{Version,Commit,Date}` ; tap Homebrew **`LaProgrammerie/homebrew-tap`** (`brew tap LaProgrammerie/tap`) avec formule poussée par GoReleaser ; secret dédié **`HOMEBREW_TAP_GITHUB_TOKEN`** | `spec-release.md` ; module Go = dépôt réel ; produit nommé AgentFlow en doc | `release.github.name: asagiri` ; URLs install manuel / brew pointent ce repo ; pas de renommage `go.mod` pour la release ; V2 : cosign, deb/rpm, scoop — hors scope |
| ADR-016 | 2026-05-20 | **Rebranding Asagiri / `asa`** : produit public **Asagiri** ; CLI et binaire **`asa`** ; état **`.asagiri/`** ; env **`ASA_*`** + fallback **`AGENTFLOW_*`** (`internal/env/compat`) ; Homebrew formule **`asa`** ; archives `asa_{OS}_{ARCH}` ; Cloudflare **`asagiri-docs`** ; `basePath` legacy **`/asagiri`** si `GITHUB_PAGES=true` | `spec-rename.md` | ADR-015 naming release supplanté ; `.agentflow/` compat lecture seule |
| ADR-017 | 2026-05-20 | **Module Go et URLs repo** : `go.mod` → `github.com/LaProgrammerie/asagiri` ; imports et ldflags alignés ; GoReleaser `release.github.name: asagiri` ; docs/CI URLs `LaProgrammerie/asagiri` ; **dépôt GitHub** encore `hyper-fast-builder` jusqu’au rename Settings (redirections automatiques) | suite `spec-rename.md` | [`docs/migration/github-rename-asagiri.md`](../migration/github-rename-asagiri.md) |
| ADR-018 | 2026-05-27 | **spec-my-A** complet : product layer + `internal/runtime` (daemon/worker/hooks) + `internal/memory` + `internal/skills` + `internal/analysis` (V1 symbol/dependency) + `internal/investigation` (go test JSON, root-cause graph) ; SDK Go `pkg/asagiri` ; CLI `daemon run`, `skills`, `memory`, `investigate graph`, `verify --investigate-on-failure` | `spec-my-A.md` §1–26 | Runtime optionnel ; hooks limités à `asa …` |
| ADR-019 | 2026-05-27 | **Runtime API + analysis layer** : API REST + Unix socket ; SDK Go/TS ; analysis §24.16 ; métriques §24.19 ; UX `--rich` §24.20 ; embeddings mémoire ; `investigate impact` ; `context-pack.md` | `spec-my-A` §24–25 | API localhost/socket uniquement ; embeddings bag-of-words déterministes (pas de modèle vectoriel externe) |
| ADR-020 | 2026-05-29 | **Trust engine local-first** : package `internal/trust/` ; artefacts sous `.asagiri/trust/<id>/` (`report.md`, `report.json`, `replay.yaml`) ; gates `verification:` dans `config.yaml` ; CLI `verify trust`, `trust gates`, `trust replay` ; pas de service trust distant en V1 | `spec-my-B` §1–6, §19, §24 | État et rapports versionnables localement ; CI via `--ci --json` |
| ADR-021 | 2026-05-29 | **Confidence ≠ correctness** : scores sur 6 dimensions agrégées depuis checks vérifiables ; rapports exposent limites, findings et risque résiduel ; gates bloquants sur seuils `min_confidence` + `required_checks` sans prétendre à une garantie formelle | `spec-my-B` §3.2, §7, §11 | UX et docs doivent éviter le langage « garanti » ; `--strict` renforce les warns en CI |
| ADR-022 | 2026-05-29 | **Execution Graph Planner local-first** : package `internal/executiongraph/` ; artefacts `.asagiri/graphs/<id>/` ; CLI `plan graph`, `plan explain`, `graph run|status|resume|visualize` ; config `execution_graph:` ; parallélisme conservateur (`max_parallel` 2, pas de conflit fichier) ; événements `graph.*` ; intégration trust/investigation sans orchestrateur distant | `spec-my-C` §1–6, §21, §24 | Plans inspectables (Mermaid/JSON) ; mode linéaire `work`/`plan` conservé ; CI via `--ci --json` |
| ADR-023 | 2026-05-29 | **Multi-agent coordination** : package `internal/coordination/` ; handoffs `.asagiri/handoffs/<id>/` ; config `coordination:` ; rôles/isolation explicites ; `DefaultAssigner` / `ScoringAssigner` + historique succès ; worktrees git (`EnsureWorktree`) ; `NodeExecutor` + stub marker testable ; événements `agent.*` ; UX terminal `FormatMultiAgentRuntime` ; budget/conflict/escalation en mémoire (lot 4) | `spec-my-D` §3–11, §18–19 | `executiongraph.DefaultRunner` appelle `GraphCoordinator` + `NodeExecutor` via `coordination.RunOptions` ; pas de duplication d’assignation |
| ADR-024 | 2026-05-29 | **Engineering Knowledge Graph** : package `internal/knowledge/` ; artefacts `.asagiri/knowledge/` ; CLI `knowledge build|query|explain|snapshot`, `impact analyze` ; build incrémental + staleness ; bridges investigation/trust/executiongraph/coordination ; config `knowledge:` | `spec-my-E` §1–25 | Voir [`docs/decisions/024-engineering-knowledge-graph.md`](../decisions/024-engineering-knowledge-graph.md) ; extracteurs V1 heuristiques (pas d’AST complet) |
| ADR-025 | 2026-05-29 | **Memory embeddings pluggables** : `internal/memory/embedder/` (`hash`, `ollama`, `cloud` opt-in) ; config `runtime.memory` ; `asa memory reindex` ; défaut `hash` (CI-safe) | `spec-phase-finale` PF-A-01 | Distinct ADR-020 ; pas de cloud par défaut ; voir [`docs/decisions/025-memory-embeddings.md`](../decisions/025-memory-embeddings.md) |
| ADR-026 | 2026-05-29 | **SDK TypeScript npm** : `@laprogrammerie/asagiri` depuis `sdk/typescript/` ; release tag `sdk-v*` ; workflow `sdk-npm-publish.yml` ; semver indépendant du binaire `asa` | `spec-phase-finale` PF-A-02 | Secret `NPM_TOKEN` ; voir [`docs/decisions/026-npm-typescript-sdk.md`](../decisions/026-npm-typescript-sdk.md) |
| ADR-027 | 2026-05-29 | **Experience Platform UI** : package `application/internal/ui/` (Bubble Tea) ; CommandBus/QueryBus vers moteurs existants ; layout minimal ; thèmes ; extension `ui:` config ; `asa` sans args → Mission Control si TTY ; coexistence `internal/tui` (facade V3) | `spec-ui.md` lots 1–6 | Voir [`docs/decisions/027-experience-platform-ui.md`](../decisions/027-experience-platform-ui.md) ; pas de logique métier dans `ui/` ; parité CLI obligatoire ; polish lot 6 (mouse resize basique, no-animation, accessibilité, docs-site 4 locales) livré |

**Note D-FULL (ADR-023 étendu, pas d’ADR séparé) :** worktrees git réels (`coordination.EnsureWorktree`), exécution nœuds (`NodeExecutor`), scoring avec `AssignmentHistory` — handoff D-WT-1…D-RUN-1.

## Log

*(Add structural tradeoffs as you go.)*
