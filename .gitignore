/.castor.stub.php
/infrastructure/docker/docker-compose.override.yml
/infrastructure/docker/services/router/certs/*.pem

# Tools
.php-cs-fixer.cache
.twig-cs-fixer.cache

# OS / env
.DS_Store
*.log
.env
.env.*
!.env.example

# Node (assets ou app Node)
node_modules/
npm-debug.log*
yarn-error.log*
.pnpm-store/

# Build / cache
dist/
.cache/
coverage/
*.tsbuildinfo

# Terraform / OpenTofu (généré par Yoimachi ou local)
**/.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.tflint.hcl.local

# Cursor local pointer to template repository (not versioned)
.cursor/template-sync.env
